pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                git 'https://github.com/ihatecodingaf/NewMavenRepo.git'
                build 'job1'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
